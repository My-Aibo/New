import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "aibo.gitbook.io",
      "cryptoicon-api.pages.dev",
      "encrypted-tbn0.gstatic.com",
      "upload.wikimedia.org",
      "187760183-files.gitbook.io",
      "dd.dexscreener.com",
    ],
  },
};

export default nextConfig;
